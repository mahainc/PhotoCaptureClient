.build/
.swiftpm/
DerivedData/
*.xcodeproj/
*.xcworkspace/
xcuserdata/

# YOLO model source files (only the bundled .mlpackage in Resources/ is committed)
*.pt
# Root-level mlpackage files from export (not the bundled one)
/yolo*.mlpackage

.worktrees/
